# Optional. Defaults to http://127.0.0.1:8002 (local voiceagent) if unset.
# Point this at the deployed voiceagent URL once it's on Render, e.g.:
# VITE_VOICE_API_BASE=https://loveholidays-voiceagent.onrender.com
VITE_VOICE_API_BASE=
